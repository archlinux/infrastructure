use chroot = no
max connections = 200
syslog facility = local5
proxy protocol = true

[packages]
        path = /srv/ftp
        comment = archlinux packages mirror
        exclude = /*-debug/ /pool/*-debug/
