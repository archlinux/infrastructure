[profile."archlinux-core"]
distro = "archlinux"
suite = "core"
architecture = "x86_64"
source = "https://mirror.pkgbuild.com/$repo/os/$arch"
excludes = ["gcc*"]

[profile."archlinux-extra"]
distro = "archlinux"
suite = "extra"
architecture = "x86_64"
source = "https://mirror.pkgbuild.com/$repo/os/$arch"
excludes = [""]
